# --- WhatsApp Cloud API ---
ACCESS_TOKEN=your_whatsapp_access_token
APP_ID=your_whatsapp_app_id
APP_SECRET=your_whatsapp_app_secret
VERSION=v18.0
PHONE_NUMBER_ID=your_phone_number_id
VERIFY_TOKEN=your_verify_token

# WhatsApp number of the clinic front-desk / staff who receives human-escalation
# summaries (appointment requests, medical-advice hand-offs, etc.).
OPERATOR_WAID=your_operator_whatsapp_number

# --- LLM provider (switch seamlessly between OpenAI and Gemini) ---
# LLM_PROVIDER=openai (default) or LLM_PROVIDER=gemini
LLM_PROVIDER=openai
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4.1-nano
GOOGLE_API_KEY=your_google_api_key
GEMINI_MODEL=gemini-2.0-flash

# --- SQLite (local demo database) ---
# Path to the SQLite database file. Defaults to clinic.db in the project root.
DB_PATH=clinic.db
